meta {
  name: Update List
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/lists/{{listId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Mercado semana atualizado"
  }
}
